version: '3.8'

services:
  cclemon-health:
    build: src                    # 🔥 從當前資料夾的 Dockerfile 建 image
    image: cclemon-health      # ✅ 自定義 image 名稱與 tag
    container_name: cclemon-health
    ports:
      - "8888:8888"
    networks:
      - cclemon-network

networks:
  cclemon-network:
    external: true
