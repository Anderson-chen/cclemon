version: '3.8'

services:
  app:
    image: ${PROJECT_NAME}:${PROFILE}
    container_name: ${PROJECT_NAME}_${PROFILE}
    ports:
      - "9000:9000"
    networks:
      - cclemon-network

networks:
  cclemon-network:
    external: true
